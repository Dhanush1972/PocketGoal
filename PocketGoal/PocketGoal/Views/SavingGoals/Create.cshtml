@model PocketGoal.ViewModels.SavingGoalCreateEditViewModel
@{
    ViewData["Title"] = "Create Saving Goal";
}

<div class="row justify-content-center">
    <div class="col-md-8 col-lg-6">
        <div class="stat-card p-4 p-md-5">
            <div class="text-center mb-4">
                <div class="brand-icon mx-auto mb-2" style="width: 48px; height: 48px; font-size: 1.4rem;">
                    <i class="bi bi-bullseye"></i>
                </div>
                <h3 class="fw-bold text-dark mb-1">Create Saving Goal</h3>
                <p class="text-muted small">Set your target purchase and timeline</p>
            </div>

            <form asp-action="Create" method="post">
                @Html.AntiForgeryToken()
                <div asp-validation-summary="ModelOnly" class="alert alert-danger rounded-3" role="alert"></div>

                <div class="mb-3">
                    <label asp-for="GoalName" class="form-label"></label> <span class="text-danger">*</span>
                    <input asp-for="GoalName" class="form-control" placeholder="e.g. iPhone 16 Pro, Bike Down Payment" />
                    <span asp-validation-for="GoalName" class="text-danger small"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="TargetAmount" class="form-label"></label> <span class="text-danger">*</span>
                    <div class="input-group">
                        <span class="input-group-text bg-light fw-bold">₹</span>
                        <input asp-for="TargetAmount" class="form-control" type="number" step="100" min="1" placeholder="80000" />
                    </div>
                    <span asp-validation-for="TargetAmount" class="text-danger small"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="TargetDate" class="form-label"></label> <span class="text-danger">*</span>
                    <input asp-for="TargetDate" class="form-control" type="date" value="@DateTime.UtcNow.AddMonths(6).ToString("yyyy-MM-dd")" />
                    <span asp-validation-for="TargetDate" class="text-danger small"></span>
                </div>

                <div class="mb-4">
                    <label asp-for="Description" class="form-label"></label>
                    <textarea asp-for="Description" class="form-control" rows="2" placeholder="Optional notes regarding this goal"></textarea>
                    <span asp-validation-for="Description" class="text-danger small"></span>
                </div>

                <div class="d-flex justify-content-between align-items-center">
                    <a asp-action="Index" class="btn btn-outline-secondary rounded-pill px-4">Cancel</a>
                    <button type="submit" class="btn btn-primary rounded-pill px-4 shadow">
                        <i class="bi bi-check-lg me-1"></i> Save Goal
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
